{# LLM feed template #}
{% for item in page.pages %}
{% if item.published %}
{{ 'ai_note'|trans }}

---
title: "{{ item.title }}"
date: {{ item.date|date('Y-m-d') }}
{% if item.tags is defined %}tags: [{{ item.tags|join(', ') }}]{% endif %}
{% if item.excerpt is defined %}excerpt: |
  {{ item.excerpt }}
{% endif %}
author: {{ site.author.name }}
url: {{ site.url }}
blog: {{ site.url }}/blog/

{% set content = item.body %}

{{ content }}

{% endif %}
{% endfor %}